#include "item.h"
#include "shader.h"
#include "camera.h"
#include "mesh.h"
#include "gamemap.h"
/*
void Item::render()
{

}*/
/*
void sGun::setValues(Vector3 _pos, int _bullets, float _scope, float _damage, eItem _type)
{
	position = _pos;
	position.y = 1;
	num_bullets = _bullets;
	scope = _scope;
	damage = _damage;
	type = _type;
	//GameMap* map = GameMap::getInstance();
	//prop = &map->prototypes[(int)type];
}

/*
void Gun::initGuns()
{
	for (int i = 0; i < MAX_GUNS; i++)
	{
		sGun* gun = &guns[i];

		gun->setValues(Vector3(), 0, 0, 0, EMPTY);
	}
}

void Gun::createGun(Vector3 _pos, int _bullets, float _scope, float _damage, eType _type)
{
	for (int i = 0; i < MAX_GUNS; i++)
	{
		sGun* gun = &guns[i];

		if (gun->type != EMPTY)
			continue;

		//if not in use, set new values
		gun->setValues(_pos, _bullets, _scope, _damage, _type);

		break;
	}
}*/
